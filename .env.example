# Supabase project credentials (Dashboard → Settings → API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Only needed for scripts/seed.ts — NEVER expose to the client or commit .env
SUPABASE_SERVICE_ROLE_KEY=
